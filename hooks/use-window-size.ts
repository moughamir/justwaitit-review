'use client';

import { useWindowSize } from '@uidotdev/usehooks';

/**
 * Hook to track window dimensions.
 * Useful for responsive layouts, conditionally rendering components based on screen size,
 * and adjusting canvas or video dimensions dynamically.
 *
 * @example
 * const { width, height } = useWindowSize();
 */
export { useWindowSize };
